version: "3.9"
services:
  web:
    build:
      context: .
    container_name: crowd-server
    ports:
    - "3000:3000"
    volumes:
    - ../db:/var/lib/postgresql/12/main/
    - ../experiments:/src/experiments
    - ../query_pages:/src/query_pages